name: Aura Constant Pulse
on:
  schedule:
    - cron: '*/5 * * * *' # हर 5 मिनट में अपने आप चलेगा
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Aura Agent
        run: python Aura_Agent.py
        
